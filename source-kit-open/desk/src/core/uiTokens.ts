/**
 * Shared UI token values for places CSS custom properties cannot reach
 * (canvas fills/strokes). These mirror the tokens in styles.css — §3.1:
 * --info is the slate used for informational marks, NEVER a signal color
 * (green/red are locked to L6/L5).
 */
export const INFO_COLOR = '#3D6B8E';
